/** Quotation preview & PDF page size — A4 landscape @ 96 DPI (width × height). */
export const QUOTATION_PAGE_WIDTH_PX = 1123;
export const QUOTATION_PAGE_HEIGHT_PX = 794;

export const QUOTATION_PAGE_PX = {
  width: QUOTATION_PAGE_WIDTH_PX,
  height: QUOTATION_PAGE_HEIGHT_PX,
} as const;
